/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'lh3.googleusercontent.com', 'next-music-nine.vercel.app'],
  },
}

module.exports = nextConfig;